# FE-BOOKING

# all book
- GET /book
- GET /book/filter -> year, category

# detail book
- GET /book/:id
- GET /book/:id/stock
- GET /book/:id/recommendation
- POST /book/borrow -> check ke db, status pending
